---
# Common vars (override via -e or vars files)
# kubeconfig: ~/.kube/config
# context: ""
# artifacts_dir: /tmp/ocp-artifacts
# Requires: kubernetes.core collection

- name: Assert prom_rules list provided
  ansible.builtin.assert:
    that:
      - prom_rules is defined
      - prom_rules | length > 0
    fail_msg: "Provide prom_rules (list of PrometheusRule objects)."

- name: Apply PrometheusRules
  kubernetes.core.k8s:
    kubeconfig: "{{ kubeconfig | default(lookup('env','KUBECONFIG')) }}"
    state: present
    definition: "{{ item }}"
  loop: "{{ prom_rules }}"
  loop_control: { label: "{{ item.metadata.namespace }}:{{ item.metadata.name }}" }
