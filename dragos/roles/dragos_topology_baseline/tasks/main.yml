---

- name: Ensure artifacts directory exists
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- include_tasks: topology_export.yml
- include_tasks: topology_diff.yml
