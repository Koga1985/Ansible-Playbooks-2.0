---

- name: Ensure artifacts directory exists
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- include_tasks: flows_export.yml
- include_tasks: flows_allowlist_build.yml
- include_tasks: flows_ngfw_template_panos.yml
- include_tasks: flows_ngfw_template_checkpoint.yml
- include_tasks: flows_drift_compare.yml
- include_tasks: flows_exceptions.yml
