---
- name: Run ise_monitor__radius_accounting
  hosts: localhost
  connection: local
  gather_facts: false
  collections:
    - cisco.ise
    - community.general
  vars_files:
    - vars.example.yml
  roles:
    - role: ise_monitor__radius_accounting
