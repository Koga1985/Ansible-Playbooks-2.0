---
- name: Run ise_guest__sponsor_portal
  hosts: localhost
  connection: local
  gather_facts: false
  collections:
    - cisco.ise
    - community.general
  vars_files:
    - vars.example.yml
  roles:
    - role: ise_guest__sponsor_portal
