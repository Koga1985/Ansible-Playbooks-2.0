---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir | default('/tmp/ot-artifacts') }}"
    state: directory
    mode: "0755"

- name: Reconcile tickets (close when risk clears) — placeholder
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/itsm_reconcile_summary.txt"
    mode: "0644"
    content: "Reconciliation pass complete — implement per ITSM API."
